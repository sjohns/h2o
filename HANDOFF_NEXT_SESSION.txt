HANDOFF CONTEXT
===============

CURRENT STATE (as of 2026-03-03)
----------------------------------
The H2O FastAPI admin workflow is complete and all 18 tests pass.

WHAT IS DONE
------------
- Excel admin workflow: validate / preview / publish fully working
- Export → import round-trip passes clean (was a known broken issue, now fixed)
- 14-column Excel schema finalized (no user-facing IDs; explicit calculated_sticks_per_bundle)
- Eagle reference strings (eagle_sticks_per_bundle, eagle_bundles_per_truckload) stored as-is
- calculated_sticks_per_bundle is admin-selected int (NOT derived from other fields)
- actual_sticks_per_truckload is independently measured int (NOT derived)
- Two new endpoints: GET /admin/data/current (browser data view), GET /admin/data/versions
- Admin and Review browser UIs fully implemented
- Versioned publish with manifest files
- ID assignment: stable matching by name, sequential IDs for new entries
- Hot-reload on publish (no restart needed)
- All documentation updated

KEY FILES
---------
- H2O/api/admin/data_import.py   — Excel importer / validator (14 columns, no IDs)
- H2O/api/admin/routes.py        — Admin endpoints (validate/preview/publish/current/versions)
- H2O/api/runtime.py             — Snapshot loader + hot-reload
- H2O/api/auth.py                — HTTP Basic auth (require_admin, require_review)
- H2O/html/admin_data.html       — Admin browser UI
- H2O/html/review_data.html      — Review browser UI
- H2O/html/js/admin_data.js      — Shared UI logic (role-gated by data-role attribute)
- H2O/html/css/admin_data.css    — Shared styles
- H2O/api/data/packing_data.json — Active snapshot (legacy JSON shape)
- H2O/api/data/versions/         — Versioned backups

EXCEL SCHEMA (14 columns, sheet: SKUs)
---------------------------------------
product_type_name, product_type_display_order, product_type_active_flag,
sku_display_order, sku_active_flag, sku_description, size_nominal,
length_feet, popularity_score,
eagle_sticks_per_bundle (str), eagle_bundles_per_truckload (str),
calculated_sticks_per_bundle (int), actual_sticks_per_truckload (int),
notes

IMPORTANT DECISIONS
-------------------
- Eagle fields are strings — "6 / 8", "9 / 12 / 12 / 16" are valid
- calculated_sticks_per_bundle is manually selected by admin, never auto-calculated
- actual_sticks_per_truckload is independently measured, never auto-calculated
- IDs are internal, assigned from existing snapshot or generated sequentially
- Users never see IDs in Excel

HOW TO RUN
----------
cd H2O
python3 -m uvicorn api.app:app --host 127.0.0.1 --port 8001 --reload
pytest -q api/tests    # 18 tests, all passing

NEXT LIKELY WORK
----------------
- Rollback endpoint to restore a versioned snapshot
- Excel template download (blank template with headers)
- Any UI polish or additional column visibility
