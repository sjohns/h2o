# Changelog

## 0.1.0
- Added FastAPI + DuckDB cloud scaffold under `python_process/cloud_app/`.
- Added admin import and client calculate endpoints.
- Added branch-and-bound parity solver port.
- Added parity checker and expanded case coverage.
- Added env-based config loading via `.env` (`python-dotenv`).

